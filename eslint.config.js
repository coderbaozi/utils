// @ts-check
import coderbaozi from '@coderbaozi/eslint-config'

export default coderbaozi(
  {
    ignores: [
      // eslint ignore globs here
    ],
  },
  {
    rules: {
      // overrides
    },
  },
)
